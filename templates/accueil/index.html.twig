{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <section>
        <div class='banner'>
            <h1>Gallery !</h1>
        </div>
    </section>
    <section>
        <div class="boucle">
            {% for post in posts %}
            <div class="card">
                <div>
                    <h2>{{ post.title }}</h2>
                    <p>{{ post.body }}</p>
                </div>
            </div> 
            {% endfor %}
        </div>
    </section>
{% endblock %}
